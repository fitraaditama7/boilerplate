apiVersion: v1
kind: Service
metadata:
  name: boilerplate-app-mysql
  labels:
    app: boilerplate-app-mysql
spec:
  type: NodePort
  selector:
    app: fullstack-app-mysql
  ports:
  - name: http
    port: 4000
    targetPort: 4000
